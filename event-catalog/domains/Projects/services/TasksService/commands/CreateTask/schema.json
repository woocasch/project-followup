{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CreateTaskCommand",
    "type": "object",
    "properties": {
      "taskId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the task being created."
      },
      "projectId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the project to which the task belongs."
      },
      "userId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the user creating the task."
      },
      "description": {
        "type": "string",
        "description": "A brief description of the task."
      },
      "timestamp": {
        "type": "string",
        "format": "date-time",
        "description": "The date and time when the task was created."
      }
    },
    "required": ["taskId", "projectId", "userId", "description", "timestamp"],
    "additionalProperties": false
  }
  