import { theme } from "@root/theme";
import styled from "@emotion/styled";

export const PageHeader = styled.h2`
    font-size: ${theme.sizes.typography.xxlarge};
    text-align: center;
    margin-bottom: ${theme.sizes.spacing.large};
`;