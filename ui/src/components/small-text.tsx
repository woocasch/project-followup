import { theme } from "@root/theme";
import styled from "@emotion/styled";

export const SmallText = styled.span`
    font-size: ${theme.sizes.typography.xsmall};
    color: ${theme.colors.text.secondary};
`;